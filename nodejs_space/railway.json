{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install"
  },
  "deploy": {
    "startCommand": "npm run build && npx ts-node prisma/seed_trails.ts && npx ts-node prisma/seed_achievements.ts && npx ts-node prisma/seed_premium.ts && node dist/src/main",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
